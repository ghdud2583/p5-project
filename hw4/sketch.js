let startColor, endColor;

function setup() {
  createCanvas(800, 500);
  angleMode(DEGREES);
  colorMode(RGB);

  startColor = color(255, 0, 0);
  endColor = color(255, 255, 0);
}

function draw() {
  ba
